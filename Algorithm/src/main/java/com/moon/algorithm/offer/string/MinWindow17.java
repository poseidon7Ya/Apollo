package com.moon.algorithm.offer.string;

import com.moon.algorithm.base.string.AbstractString;

/**
 * 包含所有字符的最短字符串
 * 输入两个字符串s和t，请找出字符串s中包含字符串t的
 * 所有字符的最短子字符串。例如，输入的字符串s为"ADDBANCAD"，
 * 字符串t为"ABC"，则字符串s中包含字符'A'、'B'和'C'的最短子字
 * 符串是"BANC"。如果不存在符合条件的子字符串，则返回空字符
 * 串""。如果存在多个符合条件的子字符串，则返回任意一个。
 */
public class MinWindow17 extends AbstractString {
    @Override
    public boolean anagrams(String s1, String s2) {
        return super.anagrams(s1, s2);
    }
}
